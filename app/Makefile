# =============================================================
# App setup shortcut
# -------------------------------------------------------------
# Supports running `make app-setup` in this directory; same as top-level target.
# Dependencies: python3, wget, unzip, tar
# -------------------------------------------------------------
.PHONY: app-setup
app-setup:
	@echo "[app-setup] running app dependency setup..."
	python3 setup.py
	@echo "[app-setup] done."