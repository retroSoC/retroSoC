# Experimental RTL

This directory retains self-owned RTL that is not integrated into the active
Mini SoC profiles. Sources here are intentionally excluded from canonical
filelists and regressions.

Promote an experimental block only by adding it to a reviewed active filelist,
updating its documentation and running the affected firmware and simulator
validation.
