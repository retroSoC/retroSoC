# =============================================================
# App setup shortcut
# -------------------------------------------------------------
# 支持在本目录直接运行 `make app-setup`，与顶层目标一致。
# 依赖: python3, wget, unzip, tar
# -------------------------------------------------------------
.PHONY: app-setup
app-setup:
	@echo "[app-setup] running app dependency setup..."
	python3 setup.py
	@echo "[app-setup] done."